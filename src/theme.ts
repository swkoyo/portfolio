import { createTheme } from '@mui/material/styles';

const theme = createTheme({
	palette: {
		text: {
			primary: '#fff'
		},
		secondary: {
			main: '#4d918f'
		}
	},
	typography: {
		fontFamily: 'Oxygen'
	}
});

export default theme;
